LC 239, lc 643 固定window size

for i ...n:

   # first maintaining queue with cur element
          # push element to queue
          depend on how you wanna manage in the queue, sometime just simple push
          
          # pop element to queue 
          check i-k >=0  & check queue is not empty and queue[0] is s[i-k]
   # when cur window has more than k element, record max value
   if i > k-1: ...
      

lc 3, lc 209, lc 76, 424
left =0

for right in range()
   while invalid condition"
      shirnk left
   update min max here

** update min/max sometime is in while loop, when the window is match with condition
sometimes is the opoosite, the windwo shrink when condision is not what we want, so we update min/max outside of while loop

TODO 
424,1438
862
